import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.001',
  appName: 'ruts-app',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
